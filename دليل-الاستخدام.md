# دليل استخدام Flutter Platform

منصة لتطوير تطبيقات Flutter من المتصفح، وبناء APK، ثم تحديث الهواتف تلقائياً عبر الشبكة (OTA).

---

## 1. ماذا تحتاج قبل البدء؟

- Windows مع **Flutter SDK** و **Android SDK**
- **SQL Server LocalDB**
- هاتف أندرويد على **نفس شبكة الواي فاي** للكمبيوتر
- على الهاتف: تفعيل **مصادر غير معروفة** / تثبيت التطبيقات من هذا المصدر

عناوين التشغيل المحلي المعتادة:

| الخدمة | العنوان |
|---|---|
| لوحة التحكم | http://localhost:5173 |
| الـ API | http://localhost:5194 |
| الـ API من الهاتف | http://192.168.1.110:5194 |

غيّر `192.168.1.110` إلى IP جهازك إذا كان مختلفاً (في `appsettings.Development.json` تحت `Flutter:PublicBaseUrl`).

---

## 2. تشغيل النظام

من جذر المشروع:

**الـ API**

```bash
dotnet run --project backend/src/API/FlutterPlatform.API/FlutterPlatform.API.csproj --launch-profile http
```

**لوحة التحكم**

```bash
cd web-dashboard
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

افتح المتصفح على: http://localhost:5173

### تسجيل الدخول

- البريد: `admin@flutter-platform.local`
- كلمة المرور: `Admin@123!`

---

## 3. إنشاء مشروع

1. من القائمة: **Projects** → **New Project**
2. اكتب اسم المشروع واسم العرض
3. اترك **Package** كما هو (مثل `com.flutterplatform.myapp_xxxxxx`)  
   هذا هو هوية التطبيق على أندرويد. لا تغيّره بعد تثبيت أول APK على الهاتف.
4. الإصدار الافتراضي: `1.0.0`
5. احفظ. تُفتح مباشرة شاشة الـ IDE

---

## 4. المحرر (IDE)

من **Projects** اضغط **Open IDE**، أو افتح المشروع بعد إنشائه.

| الزر | الوظيفة |
|---|---|
| **Save** | حفظ الملف الحالي (أو `Ctrl+S`) |
| **Build** | بناء APK بدون إرسال تحديث للهواتف |
| **Publish** | حفظ + بناء + نشر تحديث OTA للأجهزة |
| **Share APK** | تنزيل آخر APK ناجح لتثبيته يدوياً مرة واحدة |

- الملفات تظهر في **Explorer** على اليسار
- زر **+** ينشئ ملفاً جديداً تحت `lib/`
- سجلات البناء تظهر أسفل الشاشة بعد **Publish** أو **Build**
- البناء الحقيقي يستغرق عادة دقيقة إلى ثلاث دقائق
- الناتج تقريباً **16 ميغابايت** (Release لمعمارية `arm64` فقط)

عدّل `lib/app.dart` لتغيير واجهة التطبيق. لا تحذف ملفات التحديث الداخلية؛ المنصة تضيفها تلقائياً عند البناء.

---

## 5. تثبيت أول نسخة على الهاتف

التحديث التلقائي يعمل **فقط** بعد تثبيت أول APK يدوياً.

1. اضغط **Publish** وانتظر حتى ينجح البناء
2. اضغط **Share APK** وحمّل الملف إلى الهاتف
3. ثبّت الملف (اسم التطبيق: **Flutter Platform**)
4. عند أول تشغيل اسمح بـ:
   - الإنترنت
   - تثبيت التطبيقات من هذا المصدر (مهم للتحديثات اللاحقة)
5. افتح التطبيق واتركه شغالاً قليلاً حتى يسجّل الجهاز

تحقق من **Devices**: يظهر اسم الجهاز (مثل `23090RA98G`) وحالته **Online**.

### ملاحظات شاومي / ريدمي

- فعّل **USB debugging** و **Install via USB** إذا ثبّت عبر الكابل
- عند التحديث OTA قد تظهر شاشة تأكيد: اضغط **تثبيت / سماح**
- الهاتف والكمبيوتر يجب أن يكونا على نفس الواي فاي (بدون عزل أجهزة في الراوتر)

---

## 6. النشر والتحديث التلقائي (OTA)

بعد أن يكون التطبيق مثبتاً على الهاتف:

1. عدّل الكود في الـ IDE
2. اضغط **Save** ثم **Publish**
3. راقب السجل حتى تظهر رسالة نجاح البناء، مثل:

   `Built build\app\outputs\flutter-apk\app-arm64-v8a-release.apk`

4. الهاتف يفحص التحديث كل **45 ثانية** تقريباً، أو فوراً عبر SignalR
5. يحمّل الـ APK، يتحقق من SHA-256، ثم يطلب التثبيت

لا حاجة لإعادة تثبيت يدوي في كل مرة بعد أول تثبيت.

---

## 7. صفحات اللوحة

| الصفحة | ماذا ترى فيها |
|---|---|
| **Dashboard** | ملخص: المشاريع، الأجهزة، آخر البنايات |
| **Projects** | قائمة المشاريع، فتح IDE، تنزيل APK، حذف (صلاحية مدير) |
| **Builds** | كل عمليات البناء وحالتها |
| **Releases** | الإصدارات المنشورة؛ يمكن عمل Rollback لإصدار منشور |
| **Devices** | الأجهزة المسجّلة، إصدار التطبيق، آخر ظهور |
| **Settings** | عنوان الـ API وتسجيل الخروج |

---

## 8. سير العمل المختصر

```
إنشاء مشروع
    → كتابة الكود في IDE
    → Publish (أول مرة)
    → Share APK وتثبيت يدوي على الهاتف
    → تعديل الكود
    → Publish
    → الهاتف يتحدث وحده
```

---

## 9. استكشاف الأعطال

**البناء نجح لكن الهاتف لم يتحدث**

- افتح التطبيق على الهاتف وانتظر حتى دقيقة
- تأكد أن الجهاز يظهر في **Devices** وأنه Online
- تأكد أن IP الـ API في الإعدادات يطابق IP الكمبيوتر
- على شاومي أكّد نافذة التثبيت إن ظهرت

**الجهاز لا يظهر في Devices**

- التطبيق غير مثبت، أو ليس نفس الـ Package
- الهاتف ليس على نفس الشبكة، أو الـ API غير مفتوح على `0.0.0.0:5194`
- جدار حماية ويندوز يحجب المنفذ 5194

**فشل تنزيل Share APK**

- انشر مرة واحدة على الأقل قبل التنزيل

**حجم الـ APK كان ~146 ميغابايت**

- ذلك بناء Debug قديم. البناء الحالي Release لمعمارية واحدة (~16 ميغابايت)

**التطبيق لا يُثبَّت فوق نسخة موجودة**

- تغيّر الـ Package بعد أول تثبيت. ثبّت من نفس المشروع، أو احذف التطبيق ثم ثبّت من جديد

---

## 10. تشغيل الخدمات يدوياً (مرجع سريع)

```powershell
# API
dotnet run --project D:\FromBackend\backend\src\API\FlutterPlatform.API\FlutterPlatform.API.csproj --launch-profile http

# لوحة التحكم
cd D:\FromBackend\web-dashboard
npm run dev -- --host 0.0.0.0 --port 5173
```

لوحة التحكم: http://localhost:5173  
من الهاتف على الشبكة: http://192.168.1.110:5173
